- unless @league.league_entries.length >= @league.max_players
  .league-reg
    %label
      Please choose which class you wish to race in
    = reg_car_name
    = submit_tag "Register", :class => "submit #{"hidden" if @reg_cars.blank?}", "data-league-id" => @league.id, :id => "submit-entry"
    %br
    %br
    #response